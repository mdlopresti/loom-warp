# NATS MCP Server Environment Variables
# Copy this file to .env and customize as needed

# NATS Connection URL
# Format: nats://[user:password@]host:port
# Default: nats://localhost:4222
# Examples:
#   - Local development: nats://localhost:4222
#   - With authentication: nats://myuser:mypassword@nats-server:4222
#   - Remote server: nats://nats.example.com:4222
NATS_URL=nats://localhost:4222

# Project Path (optional)
# Used to determine the project namespace if not explicitly configured
# Default: current working directory
# MCP_PROJECT_PATH=/path/to/your/project

# Logging Level (optional)
# Available levels: DEBUG, INFO, WARN, ERROR
# Default: INFO
# LOG_LEVEL=INFO

# Logging Format (optional)
# Available formats: json, text
# Default: json
# LOG_FORMAT=json

# Work Queue Configuration (optional)
# Acknowledgment timeout in milliseconds (default: 30000)
# WORKQUEUE_ACK_TIMEOUT=30000

# Maximum delivery attempts before moving to dead letter queue (default: 3)
# WORKQUEUE_MAX_ATTEMPTS=3

# Dead letter queue TTL in milliseconds (default: 604800000 = 7 days)
# WORKQUEUE_DLQ_TTL=604800000
